现在已经实现的功能：
    自动创建 数据模型相关的代码：
        只需要输入数据模型的名称 和属性就能创建全套的代码

将生成的代码和通用的代码分离，这样不但代码减少冗余的代码，用户也可以对代码进行各种修改



需要实现的操作

创建模版
start输入 名字（模版名） 准备需要的文件和文件夹
model导入模版文件，要求是正常的，能运行的代码
mark标记需要替换的单个标签和数组

模版执行的操作
data请求输入替换用的数据
move移动通用的代码，或已经存在的文件
create生成多个单项替换和数组替换的实现
pop弹出生成的文件／将文件直接添加进工程，弹出使用说明

困难，不同类型的属性需要 转化 且有些地方需要插入额外的信息
如果是数组的话，要求把数组从模版中分离，另外设置
如果是空的话，提示用户在模版中加入标签
可以保留原来的数据类型转化的方法，并允许手动添加转化的类型